name: Build macOS reusable template

on:
  workflow_call:
    inputs:
      macos_version:
        required: true
        type: string
      image_format:
        required: true
        type: string

jobs:
  build-macos-image:
    runs-on: macos-15-intel
    timeout-minutes: 80

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: System Information
        run: |
          echo "Runner OS: $(sw_vers -productName) $(sw_vers -productVersion)"
          echo "Architecture: $(uname -m)"
          echo ""
          df -h $(mktemp)
          echo ""
          echo "RAM: $(top -l 1 | grep PhysMem)"
          echo ""
          echo "CPU: $(sysctl -n machdep.cpu.brand_string)"
          echo ""
          echo "Model: $(sysctl -n hw.model)"

      - name: Create macOS image
        id: create_image
        continue-on-error: true
        run: |
          if [ "${{ inputs.image_format }}" == "iso" ]; then
            sudo bash mkmaciso "${{ inputs.macos_version }}" iso
            echo "image_name=$(basename *.iso)" >> $GITHUB_OUTPUT
          else
            sudo bash mkmaciso "${{ inputs.macos_version }}" dmg
            echo "image_name=$(basename *.dmg.img)" >> $GITHUB_OUTPUT
          fi

      - name: Upload ISO artifact
        if: inputs.image_format == 'iso' && steps.create_image.outcome == 'success'
        uses: actions/upload-artifact@v5
        with:
          name: ${{ steps.create_image.outputs.image_name }}
          path: ./*.iso
          compression-level: 0
          retention-days: 15

      - name: Upload DMG artifact
        if: inputs.image_format == 'dmg' && steps.create_image.outcome == 'success'
        uses: actions/upload-artifact@v5
        with:
          name: ${{ steps.create_image.outputs.image_name }}
          path: ./*.dmg.img
          compression-level: 0
          retention-days: 15

      - name: Summary
        if: always()
        run: |
          if [ "${{ steps.create_image.outcome }}" == "failure" ]; then
            echo "## Download macOS failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "> [!WARNING]" >> $GITHUB_STEP_SUMMARY
            echo "> This runner may have network issues. Please **re-run the workflow** to try a different runner." >> $GITHUB_STEP_SUMMARY
          elif [ "${{ job.status }}" == "success" ]; then
            if [ "${{ inputs.image_format }}" == "iso" ]; then
              echo "## Successfully built $(basename *.iso)" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "> [!Important]" >> $GITHUB_STEP_SUMMARY
              echo "> This ISO image is intended for creating macOS virtual machines on Proxmox VE, QEMU, VMware, and VirtualBox." >> $GITHUB_STEP_SUMMARY
            else
              echo "## Successfully built $(basename *.dmg.img)" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "> [!Important]" >> $GITHUB_STEP_SUMMARY
              echo '> This DMG image is intended for flashing to USB drive using [Rufus](https://rufus.ie/en/#download) (Windows) or `dd` (Linux).' >> $GITHUB_STEP_SUMMARY
              echo '> Can also be used with virtual machines (requires conversion to `.vhd` for Hyper-V or `.vmdk` for VMware).' >> $GITHUB_STEP_SUMMARY
              echo '> For QEMU / Proxmox VE, attach as a raw disk image (ISO format is still recommended for a better experience).' >> $GITHUB_STEP_SUMMARY
            fi
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "---" >> $GITHUB_STEP_SUMMARY
            echo "<details>" >> $GITHUB_STEP_SUMMARY
            echo "<summary>⚠️ Do NOT click this. Seriously.</summary>" >> $GITHUB_STEP_SUMMARY
            echo "<details>" >> $GITHUB_STEP_SUMMARY
            echo "<summary>⚠️ Final warning. Close this. Walk away.</summary>" >> $GITHUB_STEP_SUMMARY
            echo "<br>" >> $GITHUB_STEP_SUMMARY
            echo "Of course you clicked. You were always going to click.<br>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "This workflow just pulled a **legit macOS installer straight from Apple's servers** — no torrents, no sketchy mirrors." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**What just happened while you weren't looking:**" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Downloaded directly from Apple — as legit as it gets" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Saved you hours of hunting broken torrents" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ No shady third-party sites, no surprise executables" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Anyway. **Do NOT** [⭐ star this repo](https://github.com/LongQT-sea/macos-iso-builder). It would be weird if you did." >> $GITHUB_STEP_SUMMARY
            echo "![star](https://raw.githubusercontent.com/LongQT-sea/mkmaciso/main/.github/star.png)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "*(...it would also be appreciated, but that's beside the point.)*" >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "> [!TIP]" >> $GITHUB_STEP_SUMMARY
            echo "> Enable [Cloudflare WARP](https://one.one.one.one/) for faster connection to Azure Blob Storage." >> $GITHUB_STEP_SUMMARY
          fi
